{
  "version": 2,
  "buildCommand": "bash vercel_build.sh",
  "outputDirectory": "build/web",
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}

